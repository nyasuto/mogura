# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業するときに参照するプロジェクト固有の規約です。

## プロジェクト概要

<!-- 1〜2 段落でプロジェクトの目的と全体像を書く -->

## 開発手法

このプロジェクトは [Ralph Loop](docs/ralph-loop.md) で進めています。1 イテレーション = 1 タスクの原則を守ってください。詳細な運用ルールは `PROMPT.md` を参照。

## ビルド・テスト

<!-- プロジェクトに合わせて書き換える -->

```
BUILD_CMD: <例: go build ./...>
TEST_CMD:  <例: go test ./...>
```

## コーディング規約

<!-- 例:
- 外部依存は追加しない（標準ライブラリのみ）
- テストはテーブルドリブンで書く
- エラーは専用型で返し、panic は使わない
- 位置情報（Pos）は全 AST ノードに最初から持たせる
-->

## アーキテクチャ

<!-- コンポーネント構成・データフロー・主要ディレクトリの役割 -->

## 禁止事項

<!-- 例:
- `tasks.md` に書かれていないタスクへの着手
- Backlog アイテムの無断タスク化
- ビルドエラーを回避するためのコメントアウト
-->

## 参照

- `docs/PRD.md` — プロジェクト要件定義
- `docs/ralph-loop.md` — Ralph Loop の運用思想
- `tasks.md` — タスク管理
- `PROMPT.md` — 各イテレーションで Claude Code に渡すプロンプト
